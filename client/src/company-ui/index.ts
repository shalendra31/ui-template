// Enforce company UI usage by re-exporting only from your internal package.
// Replace '@yourco/ui-kit' with your package name if different.
export {
  Button,
  Table,
  Badge,
  Card,
  Header,
  Sidebar,
  Footer,
  LayoutShell
} from '@yourco/ui-kit';

// If your package exposes providers/themes, export them here:
// export { ThemeProvider } from '@yourco/ui-kit';
